<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meet5.social.mapper.FraudRecordMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO fraud_records (user_id, visit_count, like_count, time_window_minutes, detected_at)
        VALUES (#{userId}, #{visitCount}, #{likeCount}, #{timeWindowMinutes}, #{detectedAt})
    </insert>

</mapper>
